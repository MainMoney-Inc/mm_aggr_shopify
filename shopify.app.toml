# Placeholder Shopify app config. Do not put client secrets in this file.
name = "MainMoney"
client_id = "replace-me"
application_url = "https://example.invalid"
embedded = true

[access_scopes]
scopes = "read_orders,write_orders"

[auth]
redirect_urls = ["https://example.invalid/api/auth"]

[webhooks]
api_version = "2025-01"

# Native payment-method listing on checkout requires Shopify Payments App
# approval. This app is intended for Partner development stores.
